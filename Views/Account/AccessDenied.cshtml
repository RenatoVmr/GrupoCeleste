@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card" style="background-color: var(--secondary-black); border: 2px solid var(--primary-red);">
                <div class="card-body p-5">
                    <i class="fas fa-lock" style="font-size: 4rem; color: var(--primary-red); margin-bottom: 1rem;"></i>
                    <h2 style="color: white;">Acceso Denegado</h2>
                    <p style="color: var(--text-light-gray); margin: 1.5rem 0;">
                        No tienes permisos para acceder a esta página.
                    </p>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-danger">
                        <i class="fas fa-home"></i> Volver al Inicio
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
